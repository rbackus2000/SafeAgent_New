import Foundation

struct AccessibilityIdentifiers {
    static let panicButton = "panicButton"
    static let appointmentsList = "appointmentsList"
    static let emergencyContactsList = "emergencyContactsList"
    static let addContactButton = "addContactButton"
}
